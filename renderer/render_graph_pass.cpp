// RenderGraphPass implementation — P2.1 skeleton.
// Passes are constructed/owned by RenderGraph; this file exists so the
// type has a translation unit for linker references. No logic yet.

#include "renderer/render_graph_pass.h"

namespace snt::renderer {

// All members are POD/std types with default constructors; no explicit
// definitions needed in P2.1.

}  // namespace snt::renderer
