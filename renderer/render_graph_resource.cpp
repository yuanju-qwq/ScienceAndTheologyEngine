// RenderGraphResource implementation — P2.1 skeleton.
// ResourceUsage / RenderResource are header-only types; this file exists
// so the module has a translation unit. P2.3 will add a resource pool.

#include "renderer/render_graph_resource.h"

namespace snt::renderer {

// No definitions in P2.1: all types are header-only.

}  // namespace snt::renderer
